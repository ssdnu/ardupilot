name: Build & Release FTS ArduPilot Firmware

# 사용법:
#   1. 이 파일을 ssdnu/ardupilot 레포의 .github/workflows/build-release.yml 로 저장
#   2. Actions 탭에서 수동 실행 (workflow_dispatch) 하거나
#      v4.6.2 태그를 push 하면 자동 실행됨
#   3. 완료되면 Releases > v4.6.2 에 아래 파일들이 올라감 (재실행 시 덮어씀)
#        FTS_FC_V5_ArduCopter.apj / ArduPlane.apj / ArduRover.apj / ArduSub.apj
#        FTS_FC_V5_EXT_COMPASS_ArduCopter.apj / ArduPlane.apj / ArduRover.apj / ArduSub.apj
#        FTS-FC-V7_ArduCopter.apj / ArduPlane.apj / ArduRover.apj / ArduSub.apj
#        FTS_FC_V5_bootloader.bin
#        FTS-FC-V7_bootloader.bin
#
#   매뉴얼 PDF에는 releases/latest 링크를 넣으면 됨:
#     https://github.com/ssdnu/ardupilot/releases/latest

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  RELEASE_TAG: v4.6.2

jobs:
  build:
    runs-on: ubuntu-22.04
    container:
      image: ardupilot/ardupilot-dev-chibios:latest
      options: --user root

    strategy:
      fail-fast: false
      matrix:
        board:
          - FTS_FC_V5
          - FTS_FC_V5_EXT_COMPASS
          - FTS-FC-V7
        vehicle:
          - { target: copter, bin: arducopter, label: ArduCopter }
          - { target: plane,  bin: arduplane,  label: ArduPlane }
          - { target: rover,  bin: ardurover,  label: ArduRover }
          - { target: sub,    bin: ardusub,    label: ArduSub }

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Fix git safe.directory
        run: git config --global --add safe.directory '*'

      - name: Configure board
        run: |
          ./waf configure --board "${{ matrix.board }}"

      - name: Build ${{ matrix.vehicle.label }}
        run: |
          ./waf ${{ matrix.vehicle.target }}

      - name: Locate build output
        id: locate
        run: |
          APJ=$(find build/${{ matrix.board }}/bin -name "${{ matrix.vehicle.bin }}.apj")
          echo "apj_path=$APJ" >> "$GITHUB_OUTPUT"

      - name: Rename output
        run: |
          mkdir -p out
          cp "${{ steps.locate.outputs.apj_path }}" "out/${{ matrix.board }}_${{ matrix.vehicle.label }}.apj"

      - name: Upload build artifact (between jobs)
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.board }}-${{ matrix.vehicle.target }}
          path: out/*.apj
          retention-days: 7

  build-bootloader:
    runs-on: ubuntu-22.04
    container:
      image: ardupilot/ardupilot-dev-chibios:latest
      options: --user root

    strategy:
      fail-fast: false
      matrix:
        board:
          - { hwdef: FTS_FC_V5,  asset: FTS_FC_V5_bootloader }
          - { hwdef: FTS-FC-V7,  asset: FTS-FC-V7_bootloader }

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Fix git safe.directory
        run: git config --global --add safe.directory '*'

      - name: Configure board (bootloader)
        run: |
          ./waf configure --board "${{ matrix.board.hwdef }}" --bootloader

      - name: Build bootloader
        run: |
          ./waf bootloader

      - name: Locate build output
        id: locate
        run: |
          BIN=$(find build/${{ matrix.board.hwdef }}/bin -name "AP_Bootloader.bin")
          echo "bin_path=$BIN" >> "$GITHUB_OUTPUT"

      - name: Rename output
        run: |
          mkdir -p out
          cp "${{ steps.locate.outputs.bin_path }}" "out/${{ matrix.board.asset }}.bin"

      - name: Upload build artifact (between jobs)
        uses: actions/upload-artifact@v4
        with:
          name: bootloader-${{ matrix.board.hwdef }}
          path: out/*.bin
          retention-days: 7

  release:
    needs: [build, build-bootloader]
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout (for commit info)
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Flatten artifacts
        run: |
          mkdir -p release-assets
          find artifacts -type f \( -name "*.apj" -o -name "*.bin" \) -exec cp {} release-assets/ \;
          ls -la release-assets

      - name: Get short commit hash
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Create / update release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ env.RELEASE_TAG }}
          name: "FTS ArduPilot Firmware ${{ env.RELEASE_TAG }}"
          body: |
            ArduPilot 4.6.2 기반 FTS 커스텀 펌웨어 (ArduCopter / ArduPlane / ArduRover / ArduSub)

            - 소스 커밋: ${{ steps.vars.outputs.sha_short }}
            - 빌드 일시: ${{ github.event.repository.updated_at }}
            - 보드: FTS_FC_V5, FTS_FC_V5_EXT_COMPASS, FTS-FC-V7
            - FTS-FC-V7 은 부트로더 board ID 2001 (구 부트로더 1010 보드는 부트로더 재플래싱 필요)

            ※ 반드시 이 릴리스의 파일만 사용하십시오. 다른 출처의 펌웨어를 올리면 보드가 정상 동작하지 않을 수 있습니다.
          artifacts: "release-assets/*"
          allowUpdates: true
          replacesArtifacts: true
          removeArtifacts: false
          makeLatest: true
